
## 进程process和线程thread
- 进程是操作系统 分配资源的基本单位。也就是说 分配资源是以进程为单位。
- 创建一个进程 就等同于 创建一个独立的内存空间，内存空间中至少有一个线程。这个初始线程一般叫主线程。

- 线程是操作系统调用CPU的基本单元。也就是说 调用CPU执行代码是以线程为单位。线程是真正执行代码的人。进程内的所有线程都共享同一块内存。




- 进程是操作系统分配资源的基本单位。创建一个进程 就等同于 创建一块独立的内存空间，这块内存空间中至少有一个线程。这个初始线程一般叫主线程。

- 线程是操作系统调度CPU执行代码的基本单位。线程是真正执行代码的人。进程内的所有线程都共享同一块内存。
- 打开浏览器会创建浏览器进程。
- 每打开一个标签页就会创建一个渲染进程。 
- 前端的代码都在渲染进程测渲染主线程上执行。所以js是单线程语言。