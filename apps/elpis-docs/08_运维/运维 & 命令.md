



## linux


## svn 常用命令
```shell

# 克隆项目 类似 git clone 
svn checkout 地址 
svn checkout svn://192.168.202.11/project/10_CODE/22_IPTV移动腾讯专区/01_客户端代码/web_new/web省内大屏epg/广东移动新Launcher/语音AI专区正式版/迭代版本/Branchs/Branch1.5.0

# 查看状态 类似 git status
svn status 

# 拉取远程最新代码 更新到最新版本（类似git pull）
svn update

# 提交代码 类似 git 的 add + commit + push 
svn add new-file.js 
svm commit -m "xx"

# 放弃当前目录及子目录的所有修改  放弃所有未提交的修改（最常用）
svn revert -R .


# 查看文件状态
svn status

# 状态标识说明：
# ?   未版本控制的新文件
# A   已添加到版本控制
# M   已修改的文件  
# D   已标记删除的文件
# C   冲突的文件
# !   缺失的文件

# 更新整个工作副本
svn update

# 更新特定目录
svn update src/

# 更新到特定版本
svn update -r 1234

# 添加新文件
svn add newfile.java

# 撤销单个文件的修改
svn revert UserService.java

# 提交所有修改
svn commit -m "修复用户登录bug"




### 遇到svn 不能删除文件  强制删除就可以推送成功
# 1. 删除本地文件（如果还存在）
rm 文件名

# 2. 从 SVN 中删除记录
svn delete 文件名

# 3. 如果提示文件不存在，使用 --force 强制删除
svn delete --force 文件名

# 4. 提交更改
svn commit -m "删除文件：文件名"
```

 

 

windows 用TortoiseSVN

## git 和 svn 
```shell

# git命令 克隆仓库
git clone 地址

# git命令 克隆指定分支
git clone -b dev 地址

# svn令 克隆仓库
svn checkout 地址 
svn checkout svn://192.168.202.11/project/10_CODE/22_IPTV移动腾讯专区/01_客户端代码
svn checkout svn://192.168.202.11/project/10_CODE/22_IPTV移动腾讯专区/01_客户端代码
svn co 地址 # 简写 

# git 提交代码
git add .
git commit -m "你的提交日志信息"
git push origin main 

# svm 提交代码  
svn add new-file.html # 添加单个文件
svn add . # 添加所有
svn commit -m "你的提交日志信息"

# git 拉取代码
git pull origin main
svn update 

# git 查看状态
git status 
svn status # svn 查看状态
```

## 末尾


## git 
## git 回滚
```javascript

1 获取 commit 哈希值
使用 git log 查看所有提交的详细信息。
使用 git log --oneline 查看简化版本的提交历史，获取 commit ID 和提交信息。
使用 git log <file_name> 查看特定文件的提交历史。

2 使用 git reset（会修改历史记录）
// 硬回滚  这种方法会将 HEAD 移动到指定的 commit，并且清除工作区和暂存区的所有更改。所有本地未提交的修改都会丢失。
git reset --hard 8485ca24fa0bea941e78ef222059d09fa165e348

// 软回滚  这会将 HEAD 移动到 <commit_id>，但是保留工作区中的修改，并且暂存区（staging area）也不会被清除。这意味着你可以稍后选择提交这些修改。
git reset --soft <commit_id>

// 软硬混合回滚 这种方法会将 HEAD 移动到指定的 commit，并且重置暂存区的内容，但保留工作区的更改。也就是说，已暂存的更改会被撤销，但文件内容依然保留在工作区。
git reset --mixed <commit_id>


3 使用 git checkout（不修改历史记录）

4 使用 git revert（保留历史记录，适合多人协作）
```





```javascript
incoming 进来的 
```



git 命令

```shell
// 克隆仓库
git clone -b 分支名 远程地址  


# 新建分支
git checkout -b 新分支名 # 基于当前分支 创建新分支并切换到新分支 （常用）
git branch 新分支名 # 基于当前分支 创建新分支 
git checkout 分支名 # 切换到指定分支
 

git branch -a  # 查看分支 简写
git branch -all  # 查看分支
git log --graph --pretty=oneline --abbrev-commit --all  # 查看分支合并图
 
# 删除分支 
git branch -d 本地分支名 # 删除本地分支 （会检查当前分支是否合并过）
git branch -D 本地分支名 # 强制删除本地分支 （不做任何检查 强制删除 （少用））
 
git push origin -d 分支名  # 删除远程分支
  
git branch -dr origin/远程分支名 # 删除追踪分支 只是删除了本地分支和远程分支的关联


# 直接删除.git 隐藏文件夹 重新git init 重新建立版本库







git clone -b master http:xx 

git checkout -b dev origin/dev // 新建本地dev分支和远程dev分支保持一致 
```

master分支（固定存在） 永远和生产保持一致 

dev （固定存在）分支 永远和开发环境保持一致  拉取master   提交到 master 



main >>> dev >>> featrue-a : featrue-a 功能分支基于dev 合并到dev  使用完 删除该分支 

main >>> hotfix-a  : hotfix-a 分支    热修复分支 基于master分支 提交到master （处理生产的问题）



1 readme文件

2 .gitignore  -- 添加git忽略文件 /node_module package-lock.json

3 pageage.json  用npm init生成   添加 开发依赖和生产依赖  npm i 生成node_modules 

4 创建index.js 项目入口文件. 用 node index.js 启动服务 

```javascript
const Koa = require('koa');

// new一个koa实例
const app = new Koa();
// 端口号
const post = process.env.PORT || 8080;
// ip地址 
const host = process.env.IP || '0.0.0.0';

// 尝试启动服务
try {
  app.listen(port, host)
  console.log(`启动服务成功：${host}:${ port }`)
} catch (e) {
   console.log(e)
}
```

5 .eslintignore  -- 添加eslint忽略文件

6 .eslintrc 



<!-- 这是一张图片，ocr 内容为：VIEW1 单页面 VIEW2 VIE3 VIEW1 VIEW2 页面A CSR-SPA 页面E 前端路由ROUTER 前端路由 ROUTER 多页面入口 展示层 页面入口A 页面入口B 页面入口C SSR-MPA 页面入口D 页面入口E 页面入口F 页面入口 框架层 MVVM 框架 UI框架 存储STORE 工程化 组件库 工具库 模板库 接口请求 接入层 ROUTER 接口路由分发 ROUTER-SCHEMA 路由规则 MIDDLEWARE路由中间件 业务层 BFF层 ENV环境分发 CONTROLLER 处理器 CONFIG 提取 SCHEDULE定时任务 EXTEND服务拓展 服务层 SERVICE 处理器 WWW...WWWWWWW.W.W.W.W.W.W.W.W.W.W.W.W.W.W.WWWWWWW........W.W.W.W.WWWWWWWWWWWWW...WWWWWWWWWWWWWWW....W 数据层 数据库 缓存 文件存储 日志 外部服务 -->
![](https://cdn.nlark.com/yuque/0/2025/png/34696752/1736067989322-98de2bf4-5dde-4319-8292-4ed17d1d6fe7.png)



<!-- 这是一张图片，ocr 内容为：项目文件 运行时 ZHEXUANQUANZHAN3 000006 APP/MIDDLEWARE/**/*.JS MIDDLEWARE APP/ROUTER-SCHEMA/**/*.JS ROUTER-SCHEMA ELPIS-CORE APP/ROUTER/**/*.JS ROUTER LOADER 1 APP/CONTROLLER/**/*JS CONTROLLER LOADER 2 APP/SERVICE/**/*JS LOADER... SERVICE APP/CONFIG/**/*.JS CONFIG APP/EXTEND/**/*.JS EXTEND -->
![](https://cdn.nlark.com/yuque/0/2025/png/34696752/1736068108046-11475d61-7fa9-4546-8f5c-b1da06fe928a.png)



<!-- 这是一张图片，ocr 内容为：运行前(磁盘文件) 解析器 运行时(内存) MIDDLEWARE(中间件)洋葱图模型 其他中间件...... 中间件2:参数校验 中间件1:错误捕获 APP/ROUTER/**/**.JS 服务引擎ELPISKOA APP/ROUTER-SCHEMA/**/**.JS ROUTER-LOADER APP/MIDDEWARE/**/**.JS ROUTER-SCHEMA-LOADER APP/CONTROLLER/**/**.JS API响应 API请求 MIDDLEWARE-LOADER APP/SERVICE/**/**.JS ROUTER(路由) CONTROLLER-LOADER 业务逻辑处理 ROUTER-SCHEMA SERVICE-LOADER APP/EXTEND/****.JS 页面响应 页面请求 EXTEND-LOADER APP/SCHEDULE/***.JS SCHEDULE-LOADER CONFIG-LOADER CONFIG/*****.JS CONTROLLER SSR CONFIG EXTEND SERVICE 页面模板 其他能力..... 调用外部服务 日志 读写 MYSQL 服务层 业务层 图例: 接入层 -->
![](https://cdn.nlark.com/yuque/0/2025/png/34696752/1736068597356-7614b257-cabd-4f61-997d-29993a555743.png)



```markdown
/**
    validate-commit-msg ghooks 
    feat: 新功能（feature）
    fix: 修补bug 
    docs: 文档（documentation）
    style: 样式（不影响代码逻辑运行的变动）
    refactor: 重构 
    test: 增加测试 
    chore: 构建过程或辅助工具的变动 
 */
```

<!-- 这是一张图片，ocr 内容为：大厂的GIT代码规范 分支命名 TEST DEVELOP HOTFIX FEATURE RELEASE MASTER RELEASE为预上线 TEST为测试环境分 线上出现紧急问 开发新功能时, DEVELOP为开发环 MASTER 为主分支 以DEVELOP 为基础 分支(预发布分 题时,需要及时 支,外部用户无 境分支,始终保 也是用于部署 创建FEATURE分支 法访问,专门给 支),UAT测试阶 修复,以MASTER 持最新完成以及 生产环境的分支 测试人员使用, 分支命名时以 段使用.一般由 分支为基线,创 BUG修复后的代码 需要确保 版本相对稳定. FEATURE/开头,后 建HOTFIX分支.修 TEST 或 HOTFIX 分 用于前后端联 MASTER分支稳定 复完成后,需要 面可以加上开发 支合并,不建议 调.一般开发的 性.MASTER 分支 的功能模块,命 合并到MASTER分 直接在RELEASE分 新功能时, 一般由RELEASE以 支上直接修改代 名示例: 支和DEVELOP 分 FEATURE分支都是 及HOTFIX 分支合 支. 码. FEATURE/USER MO 基于DEVELOP分支 并,任何时间都 DULE. 创建的. 不能直接修改代 FEATURE/CART_MO 码. DULE @籁小知 分支与环境对应关系 DEV FAT 功能 分支 环境 可访问 功能验收测 用于开发者 主分支,稳定版本 PRO MASTER 是是否 试环境,用 调试使用 DEV 于测试环境 DEVELOP 开发分支,最新版本 开发分支,实现新特性 FEATURE UAT PRO 只是是 测试分支,功能测试 FAT TEST 用户验收测 生产环境 试环境,用 预上线分支,发布新版本 UAT RELEASE 于生产环境 否 紧急修复分支,修复线上BUG HOTFIX 新增功能 FEAT @籁小知 单次提交注意事项 修复BUG FIX 仅文档更改 DOCS 1.提交问题必须为同一类别 2.提交问题不要超过3个 STYLE 不影响代码含义的更改(空白,格式设置,缺失分号等) 3.提交的COMMIT发现不符合 REFACTOR 既不修复BUG也不添加特性的代码更改 规范,GIT COMMIT--AMEND -M"新的提交信息"或GIT 改进性能的代码更改 PERF RESET--HARD HEAD 重新提交 添加缺少的测试或更正现有测试 TEST 一次 @籁小知 对构建过程或辅助工具和库(如文档)的更改 CHORE -->
![](https://cdn.nlark.com/yuque/0/2025/png/34696752/1744209301821-ee38b079-6bff-4c4e-b230-ee5494cfb273.png)

## 宝塔部署


## linux 命令




 











```shell




# 自己下载安装的命令


# 安装git得到git命令
git --version # 查看git版本
git add . # 添加所有文件到暂存区
git commit -m '提交信息' # 提交到本地仓库
git push # 推送到远程仓库
git pull # 拉取远程仓库
#...

# npm i -g @vue/cli 得到vue命令
vue create my-project -m npm # 创建项目并且指定npm为包管理工具
vue create my-project -m yarn # 创建项目并且指定yarn为包管理工具



# npm i -g @vue/cli-service 得到 vue-cli-service 命令  
vue-cli-service serve # 启动项目   serve: 'vue-cli-service serve'  # npm run serve
vue-cli-service build # 打包项目
vue-cli-service inspect # 查看webpack配置


 

# 下载node 得到 node npm npx 命令
node -V # 查看node版本 
npm run serve # 启动项目
npm run build # 打包项目
npm install # 安装项目依赖
npm i 包名 包名 -D # 安装开发依赖
npm i 包名 包名 -S # 安装生产依赖
 
node 'xxx.js' # node运行js文件  # window 需要cd到 父级目录 执行 node 文件名 # mac 任意目录 执行 node 文件路径名
 
node -e 'console.log("hello")' # 直接执行js代码

# npm i nodemon -g  # 安装nodemon命令
nodemon 'xxx.js' # 监听文件变化 自动重启node服务


npx create-react-app my-app # 创建react项目
npx @vue/cli create my-app -m npm # 创建vue项目

# npm i pnpm -g  # 安装pnpm命令
pnpm serve # 启动项目
pnpm build # 打包项目
pnpm i # 安装项目依赖


# npm i yarn -g  # 安装yarn命令
yarn serve # 启动项目
yarn build # 打包项目
yarn # 安装项目依赖
yarn add 包名 包名 -D # 安装开发依赖
yarn add 包名 包名 -S # 安装生产依赖


# npm i nvm -g  # 安装nvm命令
nvm install v14  # 安装指定版本node  注意mac系统版本号前必须加v
nvm alias default v14  # 设置默认的node版本
nvm install stable # 安装最新稳定版
nvm ls  # 查看本地node版本
nvm list # 查看本地node版本
nvm ls-remote # 查看远程node官网有哪些版本
nvm ls-remote --lts # 查看远程node官网有哪些lts版本（稳定版）
nvm current # 显示当前使用的版本
nvm -v # 查看 nvm 版本
nvm --version # 查看 nvm 版本
nvm list installed # 显示已安装的node
nvm list available # 显示所有可以下载的版本





# 统信系统 
echo $PATH # 查看环境变量 保证node对应的目录下有node包
```



## git
```shell

# 如果已经基于旧分支做了新的修改 还没有执行add  想把修改移动到新分支  怎么办？ git stash
git stash save "备注信息"
git checkout master
git pull origin master
git checkout -b 新分支名
git stash pop


```

## 命令行语法
` windows系统 使用doc命令  
  mac系统 使用linux命令

  windows系统安装git bash here 就可以使用mac系统的命令行语法  
` 



```shell
# 命令行注释 以#开头表示为注释
# 命令行粘贴
ctrl + v 或者 鼠标右键 或者 鼠标中键(滚轮) 
# 显示当前路径
pwd 
# 显示当前文件夹目录
ls 
# 去D盘(windows专属)
D:
# cd .. 退回上一级
cd ..
# cd ~ 回到电脑根路径 mac专属
cd ~
# 切换到D盘
d:
# 切换到E盘
e:
# 清空屏幕
clear 
# 清屏指令 cls是clears的简称
cls 
# 按上箭头 显示上一条命令
上 
# 打断当前命令 取消当前命令
ctrl + c

# 列出当前目录下所有文件
dir
# 进入到某个目录某个文件夹
cd 文件夹名/路径 -- windows 路径加引号

#创建某个目录路径
md 目录名
# 删除某个目录路径
rd 目录名

`.`表示当前目录

`..`表示上一级目录

` ↑ `      键盘上键 调出历史指令记录

`某某 --version`    `某某 -V`   查看某某版本号

cmd窗口停掉服务器  两次ctrl+c

# 文件夹操作
mkdir 文件夹名  # 增
rm -rf 文件夹名  # 删
mv 旧文件夹名 新文件夹名  # 重命名
ls # 查看当前文件夹的文件列表
ll # 查文件列表 

# 文件操作
touch 文件名   # 增 新建一个文件
rm -rf 文件名  # 删 删除一个文件
vi 文件名 # 改内容 编辑文件 用vi编辑器
mv 旧文件名 新文件名  # 重命名
tail 文件名    # 查看文件 (windows不行)

# 编辑文件(windows不好使) 
vi编辑器
vi 文件路径
- 1 按 i # 进入编辑模式  上下左右移动光标 修改 
- 4 按 esc # 退出编辑模式
- 5 输入 :wq  # 保存并退出         注意:是英文的 经常不切换中英文导致不能退出
- 5 输入 :wq! # 强制保存并退出
- 6 输入 :q   # 不保存退出     
- 6 输入 :q!  # 强制不保存退出

 
右击cmd窗口上方区域的属性




echo 参数  // 打印出参数
pwd // 输出当前的目录
ls // 展示当前目录中有哪些文件或目录
cd 路径 // cd可以去其他目录
mkdir 新目录名 // 创建一个新目录
rmdir 目录名 // 删除一个目录
touch 文件名.txt // 创建一个文件
rm 文件名.txt // 删除一个文件
cp 文件名.txt 路径 // 复制一个文件到指定路径
mv 文件名.txt 路径 // 移动一个文件到指定路径


open ~/.bash_profile  # 用mac默认的文本编辑器打开指定文件
cd ..
cd ~/




```



![画板](https://cdn.nlark.com/yuque/0/2025/jpeg/34696752/1754381387824-2559c388-4c4d-43ff-8dee-5b7bbf82f7e0.jpeg)



## CI 持续集成
+ git push 
+ 编译 npm run build 
+ zip打包
+ 从git push  >>> 得到一个zip包

```javascript
设计到 jenkins

git push 

1 git clone 
2 pnpm i 安装依赖
3 npm run build 编译
4 docker 打包

概念1 查看jenkins官网
概念2 查看docker官网（docker是一个容器 相当于是一个电脑 可以下载node环境等等）
概念3 镜像仓库 里面有多个docker容器 
```

## CD 持续部署
+ 上传服务器节点
+ 解压
+ pm2运行 / 放到Nginx 





```javascript


代码托管平台 

阿里云 腾讯云 亚马逊云 


1 检出代码 （git clone）
2 安装依赖  pnpm i 
3 打包构建 npm run build
4 构建docker镜像 执行Dockerfile  把docker推送到制品仓库




k8s集群管理是一个开源的容器编排 
woker节点 就是一台服务器电脑 （需要花钱购买） 会提供一个ip地址 提供给用户访问


k8s有很多用处 
  比如说 
    需要发布版本 不可能让用户有10几秒不能访问 而是分很多节点
    一部分节点先更新 对于用户来说 某些用户在同一时间点 先访问到新版本 有一些后访问到新版本而已
    这就是滚动发布  
  比如特殊节日 双12 618 流量很多 这个时期可以多增加节点 来处理请求 负载均衡

1 注册阿里云账号 
2 充钱 100元 
3 云产品 容器服务
4 创建集群（集群名名称 集群规格选最低 操作系统选） 集群需要使用钱 0.12元/h

```





## 是谁
```javascript
1 在阿里云 买 云数据库mysql  得到一个 数据库 账号密码 
  （记得开启外网地址 并且安全组设置规则 才可以连接数据库）

2 数据库实例里面有 登录按钮 点击进去 输入账号密码 登录进去


库里面就是表（表就像excel表）

新建库（elpis-bate） 有几个环境就有几个库 比如 测试是elpis_beta 生产是elpis_prod


建表 t_user  (用户表 t_前缀表示table )


建字段
 1 id 自增 主键 唯一标识一条数据
 2 user_id 唯一标识一个用户 （为什么用user_id来标识用户 而不是id 因为安全）


逻辑删除 而不是物理删除

user_id 一般是生成的uuid 	



```





## 写后端接口 步骤




```javascript
// 1 写 router-schema 


// 写 登录接口 步骤
// router-schema/login.js       校验参数
// router/login.js              设置路由
// contrller/login.js           实现方法
// servise/login.js




// router-schema/login.js     校验参数
module.exports = {
   '/api/auth/login': {
      post: {
         body: {
            type: 'object',
            properties: {
               username: {
                 type: 'string'
               },
               password: {
                 type: 'string'
               },
            },
            required: ['username', 'password']
            
         }
      }
   }
};

// router/login.js
module.exports = (app, router) => {
    const { auth: authController } = app.controller;
    router.post('/api/auth/login', authController.login.bind(authController));
}



//  contrller/login.js
module.exports = app => {
   const jwt = require('jsonwebtoken')
   const baseController = require('elpis-koa').Controller.Base(app);
   return class AuthController extends BaseController {
       async login(context) {
           const { username, password } = context.request.body;
           const { user: userService } = app.service; 
           // 调用service层的方法 查询数据库是否有该用户
           const userItem = await userSerivce.getUserItem({username, password});
           if (!userItem) {
              return this.fail(context, '账号密码错误'， 50000)
           }
      
           // 利用jwt生成一个token 
           const payload = { userId: userItem.user_id } 
           const { jwtSecreKey } = app.config; // 记录到default里面的
           const token = jwt.sign(payload, jwtSecreKey, {
              expireIn: 60 * 60 * 24 // 一天有效 
           })
           // 把token挂载到cookie上 
           const expires = new Date();
           expires.setTime(expires.getTime() + 1000 * 60 * 60 * 24)
           this.success(context, token,  {
                nickname: userItem.nickname
           })
       }
   }
}

// service/user.js    service层 因为登录是对user表的处理 所以命名user.js
module.exports = app => {
   const BaseController = require('elpis-koa').Service.Base(app);
   return class UserService extends BaseService {
        // 查询数据库
        async getUserItem({username, password}) {

            const { database } = app; // database就是extend下的database 

            // 'select * from `t_user` 
            // where username = ' + username + ' password = ' + password;
            // 当你需要使用sql时 直接google deepseak 都行  
             // 查询sql语法 查询knex语法  

            const res = await database('t_user').select('*').where({
               username, 
               password,
               status: app.status.NORMAL, // 直接写1不友好 用全局常量 
                 
            }).limit(1); 
            return res[0];
        } 
   } 
}
// app/extend/status.js   这就是一个常量记录 和前端的constant一样
module.exports = app => {
   return {
      NORMAL: 1,
      DELETE: -1
   }
}



```



## 
## 结尾 


